# Direct IR Null Pointer Experiment

This sample shows running CVEAssert directly on LLVM IR raised by `llvm-mctoll`, instead of emitting and reconciling a patch.

Run:

```bash
./build.sh
```

<small>(sample was produced by Codex 5.5 High, then hand-edited and verified)</small>
